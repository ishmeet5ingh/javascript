const myNums = [1, 2, 3, 4, 5, 6, 7, 8, 9];
 const newNums = myNums.filter( num =>  num > 4 ) 
// -> it returns the value
// -> return new array 
// -> elements are based on the condition .

console.log(newNums)